# 时习·论语日课 · 情境版 —— 上线交接文档（2026-09-22）

> 写给：下一个会话（负责设计后续开发方案）
> 来自：2026-09-22 上线会话
> 状态：**已上线**，本会话全部改动已推送 main（commit `ac30930`），GitHub Pages 部署成功
> 线上地址：**https://xuemanqingsong.github.io/lunyu-tool/v2/**（情境版）

---

## 0. 一句话现状

「时习·论语日课 · 情境版」是一个**纯静态单页工具**（GitHub Pages，无后端、无登录），每天抽 4 个生活情境（工作/家庭/待人/内心各 1）→ 用户挑中「这就是我」的那个 → 看对应一句论语 + 白话 + 深入讲解 + **今日行动（用户自选）**。2026-09-22 完成一轮产品迭代并上线。

---

## 1. 产品机制（当前线上版本）

### 1.1 核心流程
1. **抽一组**：每次从 4 个主题（工作/家庭/待人/内心）各抽 1 章，每章再随机取 1 条情境切片展示 → 4 张情境卡片（顺序打乱）。
2. **认领**：用户点一张卡 → 进入详情页，看该章原文、现代汉语翻译、出处、深入讲解。
3. **今日行动**：列出该章**全部**行动（3~9 条不等），用户**点选**一条，选中高亮；当天记住选择，再次进入同一章沿用。
4. **再来一组**：每天最多 3 组（初始 1 组 + 可换 2 次），换组后通过「第1组/第2组/…」标签**可随时翻回已抽过的组**。
5. **一天一局**：当天抽的组、选择都记在 localStorage，跨刷新/跨关闭保留；第二天自动重置。

### 1.2 数据层
- **506 章全量**（`v2/data.js`，`window.LUNYU_V2`），每章结构：
  ```
  { id, theme:[], scenes:[...], practices:[...], text, translation, source, insight }
  ```
- **scenes 与 practices 同下标一一对应**（第 N 条情境配第 N 条行动），但**页面展示时行动由用户自选**，不做强绑定（贴合感由用户完成，是产品红线）。
- 内容质量：2026-09-16~21 已完成全库独立复核 + 原文校勘 + 核心意群覆盖排查（详见「交接文档_产品开发_v2_情境版.md」历史）。

### 1.3 localStorage 键（前端自管，无后端）
| 键 | 内容 | 用途 |
|---|---|---|
| `shixi_v2_day` | `{date, groups:[...], remaining, cur}` | 当天抽的组历史（最多3组）、剩余换组次数 |
| `shixi_v2_week` | `{week, ids:[...]}` | 本周看过的情境 id（一周不重复） |
| `shixi_v2_pick` | `{date, picks:{chapterId:{sceneIdx,practiceIdx}}}` | 当天每章认领的情境下标 + 用户选的行动下标 |
| `shixi_v2_stats` | `{date:{pv,draw,again,claim,pick,feedback}}` | 使用统计（保留60天） |
| `shixi_v2_feedback` | `[{date,time,text}]` | 用户意见反馈（上限100条） |

---

## 2. 本次上线改动明细（2026-09-22，4 个 commit）

| commit | 改动 |
|---|---|
| `c1c7db0` | ①产品名改「时习·论语日课 · 情境版」（习论之间加中点 `·`，入口页/品牌/分享卡落款/下载文件名全改）；②「再来一组」后组标签切换回看（K_DAY 结构 items→groups 兼容迁移）；③页脚「意见·建议」反馈浮层（站内表单，存 K_FEEDBACK）；④使用统计（K_STATS + `?stats=1` 审核面板 + REPORT_ENDPOINT 上报接口占位） |
| `67494f8` | 「今日行动」从随机展示 1 条改为**列出全部、用户自选**（K_PICK 记住选择；抽组存储加 sceneIdx 兼容旧数据；统计新增 pick 列） |
| `d0cf685` | 意见反馈**去掉联系方式**收集（号主暂不主动联系用户） |
| `ac30930` | **移除分享按钮与分享卡**（分享机制和图卡需重新设计，先下掉；删了 223 行） |

**线上验证已通过**：名字带中点、意见反馈、行动自选、无分享代码、入口页更新。

---

## 3. 技术架构要点（后续开发必读）

- **纯静态站**：GitHub Pages，仓库 `xuemanqingsong/lunyu-tool`（公开），本地根 = `论语小工具/开发/`（git 根），分支约定 main=线上 / dev=日常（本次直接推了 main）。
- **部署**：`.github/workflows/pages.yml`，推送 main 时自动部署（触发路径含 `v2/**`）。改动后 `git push origin main` 即上线。
- **访问统计**：不蒜子（busuanzi）PV/UV，`.github/workflows/daily-stats.yml` 每天抓取写入 `stats/daily.json`——这是**站点级**访问量，与产品内使用统计（K_STATS）是两回事。
- **无后端**：所有用户数据都在各自浏览器 localStorage，号主**看不到汇总**（这是当前最大限制，见 §5 待办）。
- **分享卡曾用 Canvas**（`drawShareCard`，已删，可从 git 历史 `ac30930^` 找回）——重新设计时可参考旧实现。

---

## 4. 产品红线（勿违背）

1. 论语是**素材不是答案**——不匹配用户问题，贴合感由用户自己完成。
2. 先抽情境，挑中才看句子——顺序不能反。
3. 不区分用户、无登录、无身份。
4. 日期只用公历。
5. 平台敏感词自查：避免「签/摇/占卜」等词（workbuddy 曾因此封禁）。

---

## 5. 遗留待办 / 后续开发方向（号主关注点）

### 5.1 分享机制与图卡重新设计（已明确要做，未定方案）
- 分享按钮/图卡已移除，等待重新设计。**建议**：设计前先想清楚分享目的是什么（拉新？用户自我表达？品牌传播？），再定图卡信息层级（情境+论语+出处+品牌落款）与分享渠道（保存图片/系统分享）。
- 旧实现可参考 git `ac30930^` 的 `drawShareCard`（Canvas 1080 宽、竖版/方形两种、顶部品牌+日期、情境小字、书法体句子、底部品牌区）。

### 5.2 后台数据汇总（号主看不到跨用户数据）
- 现状：使用统计（抽组/换组/认领/选行动/反馈）和用户反馈都存在各用户浏览器本地，号主**无法远程查看**。
- 代码里已留好 `REPORT_ENDPOINT` 上报接口占位（`v2/app.js`），填入远程地址即会把事件 POST 过去。**候选方案**：
  - Formspree 等表单服务（适合反馈，不适合高频计数）
  - Cloudflare Web Analytics 等访问统计（适合 PV/UV，抓不到产品内动作）
  - 自建轻量后端（最强，但破坏「纯静态无后端」定位）
- **建议**：号主明确想要什么数据、以什么形式看，再选方案。

### 5.3 其他可优化点（未拍板，供讨论）
- 「再来一组」次数（当前每天 3 组上限）是否调整。
- 情境→行动是否要做更强绑定（当前靠用户自选兜底）。
- 分享重新设计时，是否同时做「收藏/保存今日」功能（用户反馈里已有提到想要收藏）。
- 移动端体验细节（当前为移动优先的单列布局）。

---

## 6. 给接手会话的开工指引

1. **先读**：本文件 + `交接文档_产品开发_v2_情境版.md`（历史决策与内容质量规范）+ `开发/v2/app.js`（当前全部前端逻辑，约 500 行，注释清晰）。
2. **本地预览**：`cd 论语小工具/开发 && python3 -m http.server 8173`，访问 `http://127.0.0.1:8173/v2/`；审核面板 `http://127.0.0.1:8173/v2/?stats=1`。
3. **改完上线**：`git add` + `git commit` + `git push origin main`（自动部署，无需其他操作）。
4. **记忆档案**：项目条目在 `~/.zcode/memories/PROJECTS.md`（搜索「论语小工具 v2」），SESSION_LOG 有 2026-09-09~22 的完整过程。
5. **号主待办反馈**：分享重新设计（等号主给方向）；后台数据汇总方案（等号主选）。

---

## 7. 本次会话遗留的未跟踪文件

- `开发/v2/v2_data/detect_corruption.py`：历史遗留的数据检查脚本，不属于本次上线，未提交。接手时确认是否保留/清理。

---

**一句话交接**：情境版已上线（`https://xuemanqingsong.github.io/lunyu-tool/v2/`），核心机制稳定；下一步重点是「分享机制重新设计」和「后台数据汇总方案」两个待办，方向等号主定。
